package com.immediatus.graphics.vertex

import com.immediatus.graphics.buffer.BufferObject

abstract class VertexBuffer(capacity: Int, drawType: Int, managed: Boolean) extends BufferObject(capacity, drawType, managed)

